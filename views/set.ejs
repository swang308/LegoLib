<!DOCTYPE html>
<html lang="en">

<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <meta name="author" content="SHAN-YUN, WANG" />

  <title>Lego Collection</title>
  <link rel="stylesheet" href="/css/main.css" />
</head>

<body class="font-mono">

  <%- include('partials/navbar', {page: '' }) %>

    <header>
      <div class="bg-base-200 p-10">
        <div class="hero-content text-center">
          <div class="max-w-md">
            <p class="py-6">Let's see the what you found: </p>
            <h1 class="text-5xl font-bold">
              <%= set.name %>
            </h1>
          </div>
        </div>
      </div>
    </header>

    <div class="grid grid-cols-2 stats shadow">
      <div class="stat place-items-center">
        <img class="w-full rounded-lg shadow-lg" src="<%= set.img_url %>" alt="<%= set.name %>" />
      </div>
      <div class="stat place-items-center">
        <p>
          <span class="font-bold">Set Name: </span><%= set.name %> <br />
          <span class="font-bold">Year Released: </span><%= set.year %> <br />
          <span class="font-bold">Theme: </span><%= set.theme %> <br />
          <span class="font-bold">Number of Parts: </span><%= set.num_parts %> <br /><br />
          <span id="return"></span>
          <br /><br />
          <a class="btn btn-primary btn-sm" href="#" onclick="history.back(); return false;">RETURN</a>
        </p>
      </div>
    </div>
    </div>
    </div>
</body>
<script>
  document.addEventListener("DOMContentLoaded", () => {
    fetch(`https://quotable.io/random`)
      .then(res => res.json())
      .then(data => {
        document.getElementById("return").innerHTML = `"${data.content}"<br />- <em>${data.author}</em>`;
      });
  });
</script>

</html>